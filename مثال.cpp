#include "arabic_cpp.h"

صحيح الرئيسية() {
    أخرج << "مرحباً بك في لغة البرمجة العربية!" << إنزل;
    
    ثابت صحيح الرقم_السري = 555;
    صحيح مدخل_المستخدم;

    أخرج << "أدخل الرمز السري للمتابعة: ";
    أدخل >> مدخل_المستخدم;
    
    إذا (مدخل_المستخدم == الرقم_السري) {
        أخرج << "تم الدخول بنجاح! جاري البدء..." << إنزل;
        دورة (صحيح ط = 1; ط <= 3; ط++) {
            أخرج << "تحليل الخطوة " << ط << "..." << إنزل;
        }
    } وإلا {
        أخرج << "خطأ: الرمز غير صحيح." << إنزل;
    }
    
    أرجع 0;
}
